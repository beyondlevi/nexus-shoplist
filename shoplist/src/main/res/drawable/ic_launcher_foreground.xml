<?xml version="1.0" encoding="utf-8"?>
<!-- Feeds both the launcher (adaptive foreground) and the monochrome icon, so the
     plugin's own cart glyph shows up everywhere. Inset keeps it inside the adaptive
     safe zone. -->
<inset xmlns:android="http://schemas.android.com/apk/res/android"
    android:drawable="@drawable/nexus_glyph_cart"
    android:inset="20%" />
